Beer.getAllBeer=SELECT b.beer_id AS beerId, b.manufacturer AS manufacturer, b.name AS name, b.country AS country, b.description AS description, b.price AS price, b.is_archived as isArchived, c.name as categoryName FROM beer b INNER JOIN category c ON b.category_id = c.category_id ORDER BY c.name, b.name
